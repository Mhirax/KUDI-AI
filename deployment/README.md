# Deployment

Deployment-grade artifacts: Dockerfiles for each deployable service,
Kubernetes manifests, and CI/CD workflow templates. Local infrastructure
dependency configuration lives in `/infrastructure/docker` and
`/infrastructure/kubernetes` instead.
