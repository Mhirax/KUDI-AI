# Docker (Deployment)

Production multi-stage Dockerfiles for each deployable service. All
images run as a non-root user, include health checks, and are built
for minimal final-image size (alpine/slim base images).
