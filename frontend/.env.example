# The frontend talks to the real backend only — there is no mock mode.
# Features whose backend module does not exist render an explicit
# "not available yet" state. See docs/API-CONTRACT.md.
VITE_API_BASE_URL=http://localhost:3002/api/v1
