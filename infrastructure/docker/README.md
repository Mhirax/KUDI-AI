# Docker (Infrastructure)

Base/shared Dockerfiles and compose fragments for local infrastructure
dependencies (Postgres, Redis, RabbitMQ). Deployment-grade Dockerfiles
for the apps themselves live under `/deployment/docker`.
