# Raw SQL Migrations

Reserved for hand-written SQL migrations that fall outside Prisma's
migration engine (e.g. complex partitioning, materialized views,
row-level security policies). Empty in Phase 1.
