# Prisma Migrations

Generated migration history (`prisma migrate dev` / `migrate deploy`)
will be written here automatically. Empty until the first domain model
is added to `schema.prisma` in Phase 2.
