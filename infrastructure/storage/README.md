# Storage

Object storage abstraction (S3-compatible) for KYC documents, statements,
receipts, and compliance/audit exports. Concrete provider (AWS S3,
Cloudflare R2, etc.) is configured via environment variables.
