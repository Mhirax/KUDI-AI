# Kubernetes (Infrastructure)

Shared/base Kubernetes manifests for infrastructure dependencies
(StatefulSets for Postgres/Redis/RabbitMQ where self-hosted). App
deployment manifests live under `/deployment/kubernetes`.
