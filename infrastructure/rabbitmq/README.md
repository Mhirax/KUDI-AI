# RabbitMQ

Message broker configuration for the platform's Event-Driven
Architecture: domain event publication, async job dispatch (e.g.
settlement, reconciliation), and webhook delivery queues with dead-letter
handling.
