# Exceptions

Shared domain exception hierarchy. Business-rule violations throw these
framework-agnostic exceptions; the gateway's exception filters translate
them into HTTP responses at the boundary.
