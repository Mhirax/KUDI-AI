# Events

Shared domain event contracts and the event bus abstraction used for
Event-Driven Architecture across module boundaries. Concrete event
payloads are defined within their owning module; only the base
contracts live here.
