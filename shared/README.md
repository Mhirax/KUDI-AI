# Shared Kernel

Framework-and-transport-agnostic code shared across every app, module,
and integration in the monorepo, per Domain-Driven Design's "Shared
Kernel" pattern. Nothing here may depend on a specific module's domain
logic — only the reverse is allowed.
