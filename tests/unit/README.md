# Unit Tests

Fast, isolated tests for domain entities, value objects, use cases, and
shared helpers. No network, database, or filesystem I/O — all
dependencies are mocked/faked. Target: 80%+ coverage on domain and
application layers.
