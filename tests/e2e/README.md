# E2E Tests

Black-box HTTP tests against a fully running application instance
(Supertest against a booted NestJS app / Docker Compose stack),
verifying end-to-end request/response contracts once endpoints exist.
