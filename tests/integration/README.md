# Integration Tests

Tests that exercise real infrastructure adapters (Prisma/Postgres,
Redis, RabbitMQ) using Testcontainers, verifying repository
implementations and event publication against actual dependencies
rather than mocks.
