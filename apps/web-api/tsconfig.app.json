{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "../../dist"
  },
  "include": [
    "src/**/*.ts",
    "../../modules/**/*.ts",
    "../../shared/**/*.ts",
    "../../infrastructure/**/*.ts",
    "../../integrations/**/*.ts",
    "../../gateway/**/*.ts"
  ],
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"]
}
