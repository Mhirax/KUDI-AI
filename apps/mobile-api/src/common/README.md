# Common (Mobile API)

App-specific cross-cutting concerns that are **not** shared across all apps
(app-wide shared code lives in `/shared`). Examples: app-specific guards,
interceptors, or decorators unique to this service.
